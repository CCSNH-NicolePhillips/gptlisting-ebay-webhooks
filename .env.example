# Local development environment for Netlify Dev (netlify dev)
# Copy to .env and fill values. .env is git-ignored.

# Auth mode
AUTH_MODE=auth0

# Auth0 (public values are OK in .env)
# Your Auth0 tenant domain, e.g. dev-1234abcd.us.auth0.com
AUTH0_DOMAIN=
# SPA Client ID for this app
AUTH0_CLIENT_ID=
# Optional: API audience if you use access tokens for a custom API
# AUTH0_AUDIENCE=

# Local site URL used during development
# Netlify Dev default is http://localhost:8888
APP_URL=http://localhost:8888

# AI Model Configuration
# Vision model for image analysis (format: provider:model-name)
# Supported providers: openai, anthropic, google
VISION_MODEL=openai:gpt-4o
# GPT model for text generation
GPT_MODEL=gpt-4o

# Dropbox OAuth app credentials (sensitive; keep .env private)
DROPBOX_CLIENT_ID=
DROPBOX_CLIENT_SECRET=
# Must be configured in Dropbox app settings as an Allowed redirect URI
DROPBOX_REDIRECT_URI=http://localhost:8888/.netlify/functions/dropbox-oauth-callback

# eBay API credentials (you can keep these for production testing only)
# Note: eBay RUName callbacks cannot use localhost; use your deployed site for real tests.
EBAY_ENV=PROD
EBAY_CLIENT_ID=
EBAY_CLIENT_SECRET=
EBAY_RUNAME=
# Optional marketplace
EBAY_MARKETPLACE_ID=EBAY_US
