{
  "name": "gptlisting-ebay-webhooks",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "netlify dev --live",
    "deploy": "netlify deploy --prod"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}