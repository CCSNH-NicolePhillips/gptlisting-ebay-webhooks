# Production environment variables for ebay-dropbox-uploader
# WARNING: This file contains secrets. Do NOT commit to git. Add to .gitignore before saving in a repo.

# Server
PORT=3000
APP_URL=https://your-production-host.example.com
DATA_DIR=.tmp

# Dropbox (production app credentials)
DROPBOX_CLIENT_ID=
DROPBOX_CLIENT_SECRET=
DROPBOX_REDIRECT_URI=https://your-production-host.example.com/auth/dropbox/callback
DROPBOX_LOCALE=en_US

# eBay (production)
EBAY_ENV=PROD
EBAY_CLIENT_ID=
EBAY_CLIENT_SECRET=
# The RUName / OAuth redirect for your production app (must match the value registered in eBay developer portal)
EBAY_RU_NAME=https://your-production-host.example.com/auth/ebay/callback

# Optional/store IDs for production account (if you already created these manually)
EBAY_PAYMENT_POLICY_ID=
EBAY_RETURN_POLICY_ID=
EBAY_FULFILLMENT_POLICY_ID=
EBAY_MERCHANT_LOCATION_KEY=

# App defaults
DEFAULT_MARKETPLACE_ID=EBAY_US
DEFAULT_CATEGORY_ID=177011
PROMOTED_CAMPAIGN_ID=
PUBLISH_MODE=draft
OCR_PROVIDER=none

# Notes:
# - Fill EBAY_CLIENT_ID/SECRET with your production app credentials before starting the server.
# - After you authorize the production account (via /auth/ebay), a production refresh token will be saved to the data dir (.tmp by default).
# - Keep this file out of source control. Example to add to .gitignore:
#   echo "prod.env" >> .gitignore
